{
  "name": "@cypsela/gateway",
  "version": "0.0.0",
  "description": "eth.tennis deployable site (CF Pages)",
  "type": "module",
  "private": true,
  "license": "AGPL-3.0-only",
  "author": "tabcat",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && vite build --config vite.sw.config.ts",
    "preview": "vite preview",
    "test": "vitest --dir test --config vitest.config.ts --run",
    "test:watch": "vitest --dir test --config vitest.config.ts",
    "test:e2e": "playwright test",
    "ci": "pnpm build && pnpm test"
  },
  "dependencies": {
    "@cypsela/gateway-sw-core": "workspace:*",
    "datastore-idb": "^4.0.3",
    "helia": "^6.1.3",
    "multiformats": "^13.4.2"
  },
  "devDependencies": {
    "@ensdomains/content-hash": "3.1.0-rc.1",
    "@playwright/test": "^1.59.1",
    "datastore-core": "^11.0.4",
    "esbuild": "^0.27.7",
    "jsdom": "^29.0.2",
    "typescript": "^6.0.3",
    "viem": "2.43.1",
    "vite": "^7.3.2",
    "vitest": "^4.1.4"
  }
}
